<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewNavigationAndHierarchy.Views"
             xmlns:ViewModel="clr-namespace:ViewNavigationAndHierarchy.ViewModel" x:Class="ViewNavigationAndHierarchy.Views.OrderView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="White">
    <UserControl.DataContext>
        <ViewModel:OrderViewModel/>
    </UserControl.DataContext>
    <Grid>
        <TextBlock Text = "Order View" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
    </Grid>
</UserControl>
