<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ViewNavigationAndHierarchy.Views"
             xmlns:ViewModel="clr-namespace:ViewNavigationAndHierarchy.ViewModel" x:Class="ViewNavigationAndHierarchy.Views.CustomerListView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="White">
    <UserControl.DataContext>
        <ViewModel:CustomerListViewModel/>
    </UserControl.DataContext>
    <Grid>
        <TextBlock Text = "Customer List View" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20"/>
        <!--<TextBlock x:Name="textBlock" HorizontalAlignment="Center" Margin="139,91,142,0" TextWrapping="Wrap" VerticalAlignment="Top" RenderTransformOrigin="0.061,0.188" Text="{Binding Path=message,Mode=TwoWay}" />
        <Button x:Name="button" Content="Increment" HorizontalAlignment="Center" Margin="0,210,0,0" VerticalAlignment="Top" Width="75" Click="button_Clicked"/>
        <Button x:Name="Button2" Content="Set" Click="Button2_Clicked" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,75,0,0"/>-->
    </Grid>
</UserControl>
